# Local (Herd): point to your Laravel Herd site (must match /api/v1 routes).
NUXT_PUBLIC_API_BASE=https://backendtest.test/api/v1

# Dokploy: use your public API URL, e.g.
# NUXT_PUBLIC_API_BASE=https://api.rentvly.com/api/v1
