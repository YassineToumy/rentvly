# Place mongodump archives here, e.g. bienici.archive
# Then restore with:
#   docker compose exec mongo mongorestore --uri="mongodb://root:root@localhost:27017/?authSource=admin" --archive=/dumps/bienici.archive --nsInclude="bienici.*"
